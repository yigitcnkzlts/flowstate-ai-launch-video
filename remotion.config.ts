/**
 * Remotion Configuration
 * Note: This file is optional in Remotion 4.0+
 * Configuration can also be done via CLI flags
 */

// Configuration is handled via package.json scripts
// You can add CLI flags in package.json if needed
// Example: "render": "remotion render Root out/video.mp4 --overwrite"

export {};
